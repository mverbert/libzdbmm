#! /usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_CONFIGURE_EXTRA_FLAGS := --includedir=\$${prefix}/include/${DEB_HOST_MULTIARCH} --libdir=\$${prefix}/lib/${DEB_HOST_MULTIARCH}  


